---
import BaseLayout, { type Props as BaseProps } from './base.astro'
import Meta, { type Props as MetaProps } from './meta.astro'
import './main.css'

export interface Props extends BaseProps, MetaProps {}

const { title, description, class: className, ...props } = Astro.props
---

<BaseLayout class:list={['min-h-screen flex flex-col *:w-full', className]} {...props}>
  {/* <body /> */}
  <slot />

  {/* <head /> */}
  <Fragment slot='head'>
    <Meta title={title} description={description} />
  </Fragment>
</BaseLayout>
